# sort_vis

## Run

```bash
python main.py
```

## Dependencies

- `curses` (stdlib on Linux/macOS; on Windows use WSL)
- `opencv-python` (for `test_cv2.py`)

## Output

`test_cv2.py` writes `output_from_array.mp4` (gitignored). Delete or ignore this file.
